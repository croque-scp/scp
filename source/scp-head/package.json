{
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir dist && sass assertions.scss | csso -o dist/assertions.css && cp *.html dist"
  },
  "devDependencies": {
    "csso-cli": "^4.0.2",
    "sass": "^1.77.2"
  }
}
